var Q= require('q');
var request= require('request');

module.exports = {
	getPlantData: function (name) {
		//TODO: This function is supposed to received the plant name and data, 
		//and use the to search google and fetch an image to save it in plant database
		//Uses an API
	}
}
